name: cv_scan_data
description: Data layer — repositories, remote (dio/retrofit), local (drift), sync engine
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cv_scan_core:
    path: ../core
  cv_scan_domain:
    path: ../domain

  dio: ^5.9.2
  retrofit: ^4.9.2
  json_annotation: ^4.12.0

  drift: ^2.34.0
  drift_flutter: ^0.3.0
  path_provider: ^2.1.5
  get_it: ^9.2.1
  injectable: ^3.0.0

  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  injectable_generator: ^3.1.0
  openapi_retrofit_generator: ^2.0.4
  build_runner: ^2.15.0
  json_serializable: ^6.14.0
  retrofit_generator: ^10.2.7
  drift_dev: ^2.34.0
