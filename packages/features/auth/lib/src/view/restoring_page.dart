import 'package:flutter/material.dart';

class RestoringPage extends StatelessWidget {
  const RestoringPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: CircularProgressIndicator()));
}
