name: feature_auth
description: Auth feature — login, pin setup, biometric unlock
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cv_scan_core:
    path: ../../core
  cv_scan_domain:
    path: ../../domain
  shared:
    path: ../shared
  cv_scan_ui_kit:
    path: ../../ui_kit
  flutter_bloc: ^9.1.0
  get_it: ^9.2.1
  freezed_annotation: ^3.1.0
  pinput: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  freezed: ^3.2.5
